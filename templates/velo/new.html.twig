{% extends 'base.html.twig' %}

{% block title %}New Velo{% endblock %}

{% block body %}
    <h1 class="text-center">Créer ton Vélo</h1>
<div class="container">
    <div class="form-group">
    {{ include('velo/_form.html.twig') }}
    </div>
</div>
    <a href="{{ path('velo_index') }}">back to list</a>
{% endblock %}
